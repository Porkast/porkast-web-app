VITE_API_BASE_URL=https://api.porkast.com/api
# Enable the Dodo checkout UI. Defaults to false when unset (production).
# Local development enables it via .env.development.
VITE_PAYMENTS_ENABLED=false
